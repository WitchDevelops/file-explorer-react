import React from 'react';
import '../styles/filepath.css';

export default function FilePath() {
  

  return (
    <div class="breadcrumbs">
      <p>This is where the breadcrumbs should be displayed, but something doesn't work</p>
    </div>
  );
}